# Kassa v2 — POS для кофеен, пекарен и specialty coffee

Часть общей системы ANGLE: POS, гостевые страницы, Supabase и Android.
Сайт и кабинет владельца — соседний репозиторий `../anglesite`.
Старая версия в `legacy/` — только референс; не импортировать из неё код напрямую.

## С чего начать

Прочитать [README](README.md), [индекс документации](docs/README.md) и
справочник изменяемого модуля. Текущий план —
[план завершения ANGLE](../anglesite/docs/product-completion-plan.md).
В этом файле хранятся устойчивые правила, не статус очередной фазы.

При изменении поведения обновлять соответствующий справочник и приёмочный
сценарий. Сверять даты и ограничения старых аудитов: они не являются текущим
планом и не доказывают наличие дефекта без воспроизведения.

## Главный принцип

**Скорость рабочего процесса — приоритет №1.** Каждый экран проектируется от количества тапов:

- Типовой заказ: ≤3 тапа (товар → модификатор → оплата)
- Optimistic UI на все мутации — интерфейс никогда не ждёт сеть
- Экран бариста: 1 тап = готово, без диалогов подтверждения
- PIN-переключение сотрудника ≤2 сек (автоотправка на 4-й цифре)

## Стек

- **Frontend**: React 19, TypeScript, Tailwind CSS 3, React Query v5, Zustand v5
- **Backend**: Supabase (PostgreSQL, RLS, Realtime)
- **Сборка**: Vite 8

## Архитектурные инварианты (не нарушать)

1. **Деньги — целые агороты** (`src/lib/money.ts`). Никаких float. Конвертация в ₪ только при отображении.
2. **Никогда не удалять финансовые записи.** Void/refund/скидка — это новая запись, не UPDATE/DELETE. Аудит-трейл священен.
3. **Только anon-ключ во фронтенде.** service_role не существует для клиента. RLS — реальная защита, не декорация.
4. **Изоляция по организации.** Доменная запись принадлежит org напрямую или через связь; RLS скоупит доступ через `auth_org_id()` из JWT app_metadata. Точки одной организации не являются изолированными друг от друга арендаторами; не обещать франшизную изоляцию без отдельной реализации.
5. **Totals снапшотятся в заказ** (сумма, НДС, скидки) в момент операции — не вычисляются заново из связей.
6. **Мутации идемпотентны** — client-generated UUID на операциях. Offline outbox и replay уже реализованы: сохранять их scope-изоляцию и идемпотентность, не очищать очередь ради обхода ошибки.

## Модель авторизации (двухуровневая)

1. **Устройство** = аккаунт Supabase Auth (email+password, вводится один раз при настройке кассы).
   `org_id`/`location_id` прописаны в `app_metadata` JWT функцией `bootstrap_org()` → их читает RLS.
2. **Сотрудник** = PIN внутри приложения. `verify_staff_pin()` (SECURITY DEFINER) сверяет bcrypt-хеш.
   Введённый PIN передаётся на проверку RPC; `pin_hash` не читается клиентом
   (колоночные гранты). PIN, хеши и токены не логировать.
   PIN-сессия живёт в sessionStorage (`authStore`) — закрыл браузер → касса заблокирована.

Ужесточение (staff-scoped токены): привилегированные RPC — строгий режим
(044/045); горячий поток (place/pay/open/append/queue) — мягкий режим 086:
`p_staff_session` шлётся и валидируется, NULL пока пропускается. Остаточный
компромисс: строгий режим горячего потока включается отдельной миграцией
после раскатки клиентов и опустошения офлайн-очередей.

## Доменная модель

Система поддерживает заказ у стойки и открытый счёт стола. Горячий counter-
поток остаётся приоритетом; не убирать существующий зал по старому описанию
«только кофейня». Основные поля и правила заказа:

- `daily_number` (#42 на сегодня), `type` (here/takeaway), `customer_name`
- Статусы: `open → paid → fulfilled` | `voided`
- Экран бариста — realtime-очередь по `paid`-заказам
- Номер чека выдаётся атомарно на сервере через `receipt_counters`,
  не счётчиком в памяти клиента.

## Структура, маршруты и права

Структура и маршруты описаны в [архитектуре](docs/architecture.md).
Источник маршрутов — `src/App.tsx` (POS) и `src/PublicApp.tsx` (гость).
Не добавлять сюда вторую копию таблицы маршрутов: она уже устаревала.

Учитывать роль сотрудника, действующую PIN-сессию и разрешения операции;
у кабинета — членство владельца/менеджера. Продукты и capabilities проверять
на сервере, а не только скрывать пункты интерфейса.
Модель выдачи доступа — [standalone-products](docs/standalone-products.md),
база и RPC — [database](docs/database.md).

## Дизайн-система

Светлый минимализм (Linear/Notion-like), без ярких заливок/градиентов/эмодзи в UI.
Глобальные классы в `src/index.css`: `.btn-primary/-secondary/-danger/-success/-ghost`,
`.card`, `.card-hover`, `.input`, `.badge-*`, `.page-header`.
Шрифт Inter; карточки `rounded-2xl`; кнопки `rounded-xl`; нажатие `active:scale-[0.97]`.
Существующие компоненты и `src/index.css` — источник текущего визуального языка;
legacy-дизайн не считать автоматически действующей спецификацией.

RTL: каждый экран оборачивается `dir={isRtl ? 'rtl' : 'ltr'}`; использовать
логические свойства (`ms-*`/`me-*`, `start`/`end`), не `ml-*`/`mr-*`.

## Дизайн-стандарты (соблюдать во всём новом UI)

Индустриальные правила, не вкусовщина. Проверять при написании каждого экрана.

1. **Сетка 8pt.** Все отступы/размеры/gap кратны 4, лучше 8: используем Tailwind-шкалу
   `1 2 3 4 6 8 10 12 16` (=4/8/12/16/24/32/40/48/64px). Избегать «съезжающих»
   `px-5`/`py-5` (20px) — брать `4` или `6`. `gap-3`/`p-4`/`p-6` — норма.
2. **Сенсорные мишени ≥44px** (WCAG/Apple; Material 48). POS = тач-планшет, палец в спешке.
   Интерактивные элементы (кнопки товаров, степперы ±, чипы, пункты меню) — высота
   ≥44px (`h-11`+). Мелкие иконки-кнопки ✎/✕ в админке — исключение (мышь, не касание).
3. **Контраст текста (WCAG AA ≥4.5:1).** Основной текст (названия, цены, суммы) —
   `text-gray-900`. Вторичный/подписи — минимум `text-gray-500` (не `gray-400`:
   он ~2.8:1, проходит только для третьестепенных лейблов).
4. **Иерархия — размером и весом, не цветом.** Заголовок крупнее+жирнее, вторичное
   мельче+серее. Шкала шрифта только Tailwind (`text-xs/sm/base/lg/xl/2xl`).
5. **Правило 60-30-10.** 60% фон (белый/`#eceef1`), 30% панели (серый), 10% акцент
   (тёмная CTA `bg-gray-900`). Не разбавлять акцентными заливками.
6. **Близость (гештальт).** Отступ ВНУТРИ группы меньше, чем МЕЖДУ группами.
7. **Внешний отступ ≥ внутреннего** — контейнер от края не теснее, чем контент внутри.

## Запуск

```bash
npm install && npm run dev
```

```env
VITE_SUPABASE_URL=...
VITE_SUPABASE_ANON_KEY=...
```

Для повседневной разработки и проверок — [development](docs/development.md).
Миграции добавляются новыми файлами; применённые миграции не переписывать.
Production-операции выполняются через проверенные project-ref guard обёртки
`npm run db:push` и `npm run functions:deploy`, по [deployment](docs/deployment.md).
Несовпадение целевого проекта — остановка, не повод обходить guard.

## Терминал и проверка

**Целевое устройство:** T2 Mini POS Terminal (Sunmi-класс). Экран 11.6", **1920×1080
ландшафт**, **Android 7.1**, встроенный термопринтер 80мм, 2D-сканер, NFC, Wi-Fi, 4G.
Юзер тестирует POS на нём.

Legacy-бандл, runtime capability-check, per-device настройки и Android-мост
печати уже существуют. Не реализовывать их повторно по старому плану.
Путь печати и ограничения — [android-printing](docs/android-printing.md).
Цель транспиляции Chrome 52 не гарантирует запуск или корректность на устройстве.
Изменения горячего потока, offline и печати требуют
[прогона на реальном терминале](docs/t2-smoke-test.md).

При изменении кода: lint, test:run, check:schema, build, check:bundle; при
изменении SQL — локальные pgTAP. Для правок только документации проверить
ссылки и `git diff --check`; это не новая функциональная приёмка.

Cardcom остаётся отложенной интеграцией. Не заявлять налоговое соответствие
по факту наличия чеков или экспорта: [регуляторная матрица](docs/israel-compliance.md).
