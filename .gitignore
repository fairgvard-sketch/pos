# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local

# Reference mockups / scratch screenshots (not part of the app)
/*.png

# Android wrapper build artifacts
android/.gradle
android/build
android/app/build
android/local.properties
# Ключи подписи и keystore — НИКОГДА не коммитим (только через секреты CI)
android/*.keystore
android/*.jks
# gradle-wrapper.jar скачивается в CI под закреплённую версию (см. workflow)
android/gradle/wrapper/gradle-wrapper.jar

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase CLI artifacts
supabase/.temp/
supabase/.branches/
